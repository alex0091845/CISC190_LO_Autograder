- link a rubric rating to a rubric rating from another submission
- have a preferences file that has:
    - default course/last course on open
    - 

- As a developer, I can implement a single, consistent way to extract report file names
  so that the code is cleaner and more maintainable. ✅

- As a developer, I can add (file) caching to each student's evaluation results so that
  if the user wants to re-run the program with different settings, they don't necessarily
  have to re-evaluate each LO for each student. ✅

- As a developer, I can test each individual run buttons for each workflow step so that
  I can ensure that each step works correctly when executed individually. ✅

- As a user, I can set the module number so that the program only runs the grading and
  rubric marking for the LOs for all modules up to that module. ✅

- As a user, I can set the module number so that the program only generates the report
  for the LOs for all modules up to that module. ✅

- As a developer, I can ask AI to look through the codebase and suggest whether certain
  ids should be a string type or an integer type and then change the codebase accordingly
  so that the code is more consistent and easier to work with. ✅

- As a user, I can easily customize the options before running the program
  (e.g., whether to grade submissions, which students to grade, etc.) so that
  I can efficiently use the program without having to modify the code. ✅

- As a developer, I can set up the AppView such that it allows the user to customize
  the options for running the program so that the user can efficiently use the program
  without modifying the code. ✅

- As a developer, I can set up an App object so that the program maintains
  state across different functions and modules, allowing for better organization
  and easier access to shared data. ✅

- As a developer, I can establish a composition relationship between App and
  AppView so that the App can manage the overall application state while the AppView
  handles the user interface, leading to a more modular and maintainable codebase. ✅

- As a user, I can easily fetch the list of students for a given course so that I can
  select them. ✅

- As a developer, I can cache the list of students for a given course so that the user
  does not have to fetch it repeatedly. ✅

- As a developer, I can fix the report view issue so that it correctly displays the
  report for the selected student. ✅

- As a developer, I can refactor the app such that each workflow step is a function
  that goes through all students so that each workflow step can be executed individually. ✅

- As a user, I can reload the configuration files while using the app so that I can 
  utilize the cached data without having to restart the app.

- As a user, I can cancel the syncing process while it's running so that I can stop it
  if I realize I made a mistake in the settings.

- As a user, I can customize how to calculate the on track grade so that I can use
  different grading schemes.

- As a user, I can change the settings from the GUI and it will update the configuration
  files accordingly so that I can easily manage the settings without having to manually
  edit the configuration files.

- As a user, I can customize the report template so that I can generate reports to my liking.

- As a user, I can mark rubric and generate report steps for a single student even only with the
  cached data so that I don't have to wait for network calls to grade submissions if I just want
  to generate a report for a single student.